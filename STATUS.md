# STATUS — Oravan

**Updated:** 2026-06-07

## Last shipped
- **App-wide UI cohesion pass (PR #52) — merged.** Brought **every non-landing route** onto the locked design system. Three new shared primitives (`PageHeader`, `EmptyState`, `Alert` [oxblood/moss, replacing 7 off-palette red/green banners]); **serif page titles** app-wide (was sans `font-bold` faux-bold); the `OravanWordmark` wired into the app sidebar/auth/onboarding/legal; **`RepCard` + `ImpactMetrics` fully retoken-ed** — which fixed a **shipped bug**: `RepCard`'s Call button used the undefined `bg-action-500` so it rendered **with no fill** (now `bg-signal` + lucide `Phone`); **neutral party pill** (deleted `partyColor()`); `Card` radius → `rounded-xl`; type-scale sweep (no raw `text-xs`); lucide-only icons. **Bill card unified on the decode-led V4** across `/bills` **and** `/dashboard` (was classic — supersedes the PR #50 "dashboard stays classic" note). A "match-the-app" mockup (feed→classic, hero→white) was **tested and rejected** via Impeccable `/critique` (it inverted *Decode-don't-display* + erased the warm Decoded hero); the warm `bg-paper-dark` hero stays. Process: cohesion → `detect` (0 findings) → `/critique` (29/40, isolated LLM + detector) → applied fixes → `/impeccable polish`. **Gate: lint + build + vitest 21/21 + Playwright 10/10 + `detect` 0 + security-sentinel merge-safe (0 findings) + UI-only confirmed (zero backend/migrations/auth/routing/data/config).** Decisions: `docs/DESIGN_DECISIONS.md → App routes UI cohesion pass` (+ post-critique section). Closes `docs/deferred.md#consolidation-followup-offscope-slate-and-semantic-colors`. **Landing untouched.** Deferred follow-ups: dashboard teaser count (10→3-4), bare-text loading vs the bill-detail skeleton, the always-identical "Federal" feed pill, a `creative-director` ceiling pass.
- **Donor-ready landing redesign (PR #51) — merged.** Full redesign of the public landing (`/`, `app/page.tsx`): scope-honest copy (dropped the web-push "Alerts" card + unbuilt-feature over-claims), real `/privacy` + `/terms` routes, the **Oravan wordmark + favicon wired** (nav `h-9` / footer `h-7`; `app/icon.svg` + generated PWA icons fix the manifest 404s), and an **authed→`/dashboard` redirect** (a logged-in visitor never sees the marketing page — this is why the site looked "unchanged" until viewed incognito post-deploy). Then a full floor+ceiling pass validated by an **Impeccable critique (detector 0 findings, Nielsen 30/40)**: center/left/left/center alignment system, tightened section seams (~160→~80px), **"How it works"** recomposed from a centered 3-up icon grid into a numbered editorial ruled list (serif numerals), **"What Oravan does"** from 5 identical shadow cards into a card-less hairline list (6 items incl. a "On the roadmap" state/local tag), CTA-label unification, closing-CTA inclusivity rewrite, AI-review safeguard moved to first mention. **Accent color explored (signal-orange → antique gold → true-complement berry) and parked — ships monochrome** (`docs/deferred.md#brand-accent-color-pops`; the `accent` token + Button variant reverted net-zero). `tests/landing.spec.ts` happy-path (logged-out render + authed redirect) **2/2**; lint + build green. Decisions: `docs/DESIGN_DECISIONS.md → Screen: Landing`. **Verified live on oravan.org** (incognito). Pre-launch flags carried forward: footer `mailto:hello@oravan.org` must exist; `noindex` block untouched.
- **V4 bill-feed card (PR #50) — merged.** Wired the locked **V4 "title-led + Decoded container"** card into `BillCard`, shipped to **`/bills` only** behind a `variant?: 'classic' | 'v4'` prop — `/dashboard` stays on the classic card (proven byte-identical), `compact` retained as the seam for a future compact variant. The card leads with the AI **headline** (`bills.ai_headline`) in a faint `bg-paper-mid` Decoded container (degrades headline → summary → "still decoding" pending), with the quiet serif title + citation floated top-right (clamped via `max-height`, **never `-webkit-line-clamp`** — preserves the float in WebKit), vote pill + **match-only interest pill** (`matched_tags` → `INTEREST_CATEGORIES` label, client-only — no RPC change). New migration **`20260606200352_feed_add_ai_headline`** `DROP`+`CREATE`s both feed RPCs to add `ai_headline` to their return shape (a column add can't use `CREATE OR REPLACE`); **applied to prod via `supabase db push`** (the clean path post-reconcile — ledger version = file prefix, no mismatch), verified the RPCs return real headlines (480/482). Tokenized 3 values (`paper.mid` / `max-w-category` / `max-h-title`). Verified WebKit + Chromium at 390 + desktop (all 3 Decoded states + the float) + e2e happy-path; lint + build green. `docs/deferred.md#feed-card-v4-build` → BUILT. **Coverage note:** the e2e asserts the degrade state on the real feed; the filled-headline + interest-pill paths are mockup/live-data verified, not yet auto-tested.
- **Settings interest-save error handling (PR #49) — merged.** `settings/page.tsx` ran `setSaved(true)` unconditionally and never checked the `user_interests` insert error, so a failed save showed a green "✅ Saved!" while nothing persisted; the missing-session case returned silently (button stuck on "Saving…"). Now mirrors onboarding's pattern: an `error` state + red banner, `{ error }` captured from **both** the `profiles` upsert and the `user_interests` insert (failure → message + clear `saving` + return before success), and `router.push('/login')` on a missing session. **Diagnosis context:** prod `user_interests` was empty because migration 007 **wiped** it (2026-06-03) and nobody had re-onboarded since (latest onboarding 2026-06-01) — the save path itself **works** (verified live: a real save landed 4 rows). Happy path / RLS / `user_interests` unchanged. lint + build green.
- **Migration-history reconcile (PR #48) — merged.** Migrations 007/008/009 were applied to prod via the MCP `apply_migration` path, which recorded them under **timestamp** versions (`20260603133555` / `20260605182345` / `20260606152313`) instead of their `007/008/009` file prefixes (the way `db push` records 001–006) — so a future `supabase db push` would treat all three as pending and re-run them, and 007's unconditional `DELETE FROM user_interests` would re-wipe interests. Fixed **zero-prod-write (Option 1)**: renamed each file so its prefix equals the recorded timestamp, restoring the file↔version link so `db push` sees them as already-applied. Also hardened 007's `DELETE` with a whitelist guard (no-op on replay) and recorded the one-time rename exception in `docs/solutions/migration-numbering.md`. Verified read-only first: `user_interests` empty (guard removes nothing) + timestamps matched char-for-char. **No prod/ledger write.** `docs/deferred.md#migration-history-version-mismatch` → RESOLVED.
- **AI-headline backfill (PR #47) — merged.** Adds `bills.ai_headline` (**migration 009**, `ADD COLUMN IF NOT EXISTS`, applied to prod via MCP) — the single editorial line the locked **V4 feed card** leads with — and a one-off `scripts/backfill-headlines.ts` that generates it **from `bills.ai_summary`** (not bill text): format "Topic Label — Action", Title Case, strictly non-partisan, ≤90 chars, naming the specific agency/rule so near-identical CRA-disapproval bills get distinct headlines. **480/482 bills now carry a headline** (the same 2 with no `ai_summary` — `hr-8330-119`, `hr-8553-119` — stay null); **measured cost ~$0.98.** Quality guards: code-level `isLeak()` (rejects multi-line / >130-char reasoning leaks, never writes them) + `clampLength()` (truncates at a word boundary, guaranteeing ≤90); verified **0 lead with "Congress", 0 bare "Resolution", 0 over 90**. The **steady-state headline pipeline is deferred** (no sync-time generation — new synced bills land headline-null; `docs/deferred.md#feed-card-v4-build`), and the **BillCard V4 build** that consumes this column is not yet wired (spec in `docs/DESIGN_DECISIONS.md`). App + migration; Codex P1 (idempotency) addressed pre-merge.
- **Feed load-more pagination (PR #46) — merged.** The `/bills` feed fetched a single 30-row window and never paged further, leaving ~450 of 482 bills unreachable — now resolved. Two parts: **migration 008** appends a unique tiebreaker (`b.id`) to both feed RPCs' `ORDER BY` (the sort was non-total — 482 bills → 183 distinct sort keys, largest tie 83 — so offset paging **duplicated/skipped** tied rows even on a static corpus); and a frontend **"Load more"** wrapper (`components/BillsFeed.tsx`, page size 30) paging the next offset window of the same RPC, for both default and personalized modes. **Verified post-migration: default 482/482, personalized 42/42, 0 duplicate-key warnings, no RPC errors.** **Gated-apply flow:** 008 was applied to prod **before** the frontend merged (gated apply → verify → merge), so prod briefly led `main` by one migration — reconciled by the #46 merge. This is the intended sequence for gated schema changes, noted so it isn't later mistaken for drift.
- **AI-summary backfill (PR #45) — merged.** One-off `scripts/backfill-summaries.ts` fills `bills.ai_summary` from each bill's Congress.gov full text (prompt/model carried from the prewarm script; generalized with null-set selection, status-DESC ordering, idempotent skip-if-set, a hard cost ceiling, Congress.gov retry + SDK `maxRetries:8`). **480/482 bills now have a summary** (461 written here + 19 pre-seeded by `prewarm-bills.ts`); **measured cost $5.21** across sample + full + mop-up. The 2 left null — `hr-8330-119`, `hr-8553-119` — have no Formatted Text on Congress.gov, so they keep the Decoded "still translating" state. The **steady-state summarize cron is still deferred** (`docs/deferred.md#steady-state-summarize-cron`). App-only, no migration; lint + tsc green.
- **Feed card identifier (PR #44) — merged.** `BillCard` rendered an empty `bill_number` (not in the feed RPC return shape); now renders `full_identifier` as a Congress citation ("S.J.Res. 139") via `billIdentifier`/`formatBillIdentifier` lifted into `lib/utils.ts`. App-only, no migration; closes `deferred.md#feature-3-bill-number-missing-from-feed-rpcs`. lint + build + `tests/bills.spec.ts` green.
- **Feature 7 — Your Impact (PR #43) — merged.** Personal activity surface at **`/impact`**: per-user **Calls made** + **Scripts generated** counts and a **reverse-chron call history** (one row per `call_event` → bill title + rep name resolved from the stored `bill_id`/`representative_id` FKs, each row linking to `/bills/[id]`), plus a no-calls empty state. Swaps the dead **`/callenge` nav tab → `Your Impact`** (`/impact`, lucide `Activity`). Scope held tight: **no** followed-bills/Follow button (no `followed_bills` writer yet — its own deferred step), scripts **count-only**, `ImpactMetrics` left untouched. Committed Playwright happy-path asserts the rendered **title + rep name** (guards embed cardinality) + the row→detail navigation + the empty state. lint + build + full suite **8/8 green**. Two deferred items landed straight to main this session: `call-events-cascade-durability`, `e2e-tests-seed-live-prod`.
- **E2E server isolation (PR #42) — merged.** Test-harness fix: `playwright.config.ts` ran the app-under-test on the default port 3000 with `reuseExistingServer: true`, so a standalone `next dev` on `:3000` (started without the mock env vars) silently shadowed the mock-wired server — which is why `representatives.spec` failed on clean `main`. Now on a **dedicated e2e port (3100)** + **`reuseExistingServer: false`**, so Playwright always starts a fresh mock-wired server and errors loudly on a clash. **Diagnosed as harness-only — NOT a Feature 2 regression; real users were never affected.** Full Playwright suite **6/6 green** including `representatives.spec`. Config-only, no app code. `docs/deferred.md#representatives-e2e-failing` → RESOLVED; hardening follow-up logged (`rep-lookup-cache-inconsistency`).
- **CRS taxonomy re-anchor (PR #41) — merged, migration applied, bills backfilled.** Replaced the 10-category / 34-subcategory keyword taxonomy with **12 flat categories**, each mapping 1:1 to Congress.gov CRS Policy Areas (all 32 areas covered; a module-load lock enforces it). `bills.policy_area` captured (**migration 007 applied — G3**, which also **wiped `user_interests`** as the pre-launch reset); all bills re-tagged via the **G4 backfill** (run through the real production tagger). **Tag coverage 78% untagged → 0.2%** — 481/482 carry a flat category tag (479 via Policy Area, 2 via keyword fallback), **0 on old ids**; **1 untaggable** bill (`hr-8330-119` — no Policy Area, no keyword match) shows in the default feed only. Feed RPCs unchanged (they intersect generically on the ids). Closes the `issue_tags` coverage gap. **Sync cron remains disabled** (`schema-drift-sync-bills`), so nothing auto-re-tags. Docs: `docs/deferred.md#taxonomy-crs-reassess`.
- **Feed floor consolidation (PR #39) — merged.** Floor pass on the bill feed (`/bills`), propagating the locked `/bills/[id]` vocabulary onto `BillCard` + `bills/page.tsx`: status pills → neutral ring-outline (`rounded-pill border-divider text-meta uppercase`; urgency color dropped; 🇺🇸 → text-only "Federal"), `Take action →` → lucide `ArrowRight`, empty-state 📋 → lucide `ClipboardList` (matching the dashboard), `slate-*` / raw sizes → tokens + type scale. **Out of scope by design:** the mono citation (`bill_number` slot left empty — the feed RPCs return `full_identifier`, not `bill_number`; deferred), card hierarchy, relevance badge, ceiling. lint + build + `tests/bills.spec.ts` green; verified at 390 + desktop via a throwaway mockup scaffold (stripped before commit — the PR is the two files only).
- **Process guardrails (PR #36) — merged.** Two scope-creep rules added to `CLAUDE.md → Hard "Do Not Do" Rules`: (1) no jumping ahead in a gated/multi-step task — finish each step and wait for sign-off; (2) no unrequested scope — no "while I'm here" extras, and when told to stop a behavior, stop and make the change directly. Repo side of a tooling/housekeeping pass that also added a global `/pr` skill and a global execution-transparency rule (both live in `~/.claude/`, outside this repo). Docs-only. **Not done this session: GitHub MCP** — deferred as its own next step.
- **Bill-detail ceiling (PR #34) — merged.** Ceiling design pass on `/bills/[id]`, locking the flagship as a **calm neutral editorial article**: contained `max-w-2xl` reader's column, the "Decoded" hero card given generous `px-12 py-14` air, official title quieted to a `text-h3`/`ink-50` reference caption (full, no clamp — the title is the legal object). One motivated rule-break (via the **creative-director** skill): the hero's label is a **serif "Decoded" at display scale** (`text-h2`) — the signature Instrument Serif carrying the hero. **No accent color** — fan-out-tested (signal orange + a teal alt) and dropped; color earned nothing. AI-summary **disclaimer designed into the card**. Every decision (incl. what was tested + rejected) recorded in `docs/DESIGN_DECISIONS.md → Ceiling pass — LOCKED`. lint + build + bill-detail Playwright green.
- **AI-summary pre-warm (PR #31) — merged.** `scripts/prewarm-bills.ts` generates plain-language Decoded summaries from each bill's **full text** (Congress.gov `/text`, Sonnet) for a curated ~20-bill sample, written straight to `bills.ai_summary`. Ran once: **19/19 written, ~$0.34.** A design/demo accelerant — NOT the production pipeline. Doc follow-ups: PRs #30/#32/#33 (migration-integrity note, prewarm follow-ups, summary-prompt length constraint).
- **Bill-detail floor (PR #29) — merged.** The floor pass the ceiling built on (slots, every state, neutral hierarchy on tokens).
- Earlier: Design Batch 1 (PR #22); Features 4 & 5 (PRs #13/#14); fixes #15–#21.

## 🚩 Flagged — steady-state summary pipeline still deferred
The Decoded hero now renders **real plain-language translations on 480/482 bills** (backfilled via `scripts/backfill-summaries.ts`, PR #45). But: (1) the **steady-state summarize pipeline was never built** — `ai_summary` is written only by the one-off scripts, so any bills a future sync ingests will land with `ai_summary` null (the backfill is a bridge, not the pipeline); (2) **summary accuracy is unverified at scale** — bills over the ~60k-char cap (e.g. `hr-1`) were summarized from a truncated slice and want a spot-check against Congress.gov. Tracked: `docs/deferred.md#steady-state-summarize-cron` and `docs/deferred.md#feature-3-prewarm-demo-bills`.

## Branch state
`main` at the **#52** merge (`7dac53b`, app-wide UI cohesion pass), preceded by #51/#50/#49. `feat/app-ui-cohesion` **merged + pruned** (local deleted; remote auto-deleted on merge, along with the stale `feat/donor-ready-landing` remote head). **`main` == `origin/main`.** Remaining branches: **`feat/landing-redesign`** (checked out in a separate worktree at `../be-the-change-landing` — active landing work, not deletable from here) and the **parked `fix/pwa-manifest`** (1 unmerged commit — the PWA `/manifest.webmanifest` fix; its own explicit-go PR, not yet opened). #52 was **UI-only** (no schema). **Migrations still applied to prod through `20260606200352`** (007/008/009 via MCP + `20260606200352_feed_add_ai_headline` via `db push`). The sync cron stays disabled.

## Feature status (7 original consumer features — Web Push deferred post-MVP 2026-06-05, so 6 ship in V1)
1. Account + profile — built. Interest-save **verified working end-to-end** (live save landed rows) and now **surfaces failures** instead of faking success (PR #49). **Email verification deferred to pre-launch** (Confirm-email OFF → no ownership check; BLOCK before public beta, `docs/deferred.md#email-verification-deferred`). Still missing account-delete (GDPR); onboarding "skip" ungated.
2. Rep lookup — built; **e2e happy-path green** (address → 3 reps via the mocked Congress/Civic path). The prior `representatives.spec` failure was a test-harness issue (server reuse), fixed in PR #42 — not a Feature 2 bug.
3. Bill feed — built. `/bills/[id]` is **floor + ceiling complete** (PR #34); the **feed list (`/bills`) is now floor-consolidated onto tokens** (PR #39 — neutral pills, lucide, type scale). Relevance now works across ~all content — the `issue_tags` coverage gap is **resolved** by the CRS re-anchor (PR #41: 12 flat CRS-anchored categories; 78% → 0.2% untagged). The feed now **paginates** (load-more, PR #46 + migration 008 tiebreaker), so all 482 bills are reachable, not just the first 30. The feed card itself is now the **V4 "title-led + Decoded container"** design (PR #50 — headline-led, shipped to `/bills` via the `variant` prop; **propagated to `/dashboard` too in PR #52**, so one decode-led card app-wide).
4. AI call script — **done (PR #13).** `ai_summary`: **backfilled to 480/482 bills (PR #45)**; the steady-state sync-time generation is **still deferred** (see flag).
5. 1-click calling — **done (PR #14).** `/api/calls` writes `call_events`; CallFlow; full-loop + no-reps specs.
6. Web push — **DEFERRED to post-MVP (decision 2026-06-05) — not a V1/MVP feature.** Schema in place (`push_subscriptions`, `notifications_sent`) but nothing built; coupled to re-arming the sync cron. V1 ships the rest of the loop without it. See `docs/deferred.md#web-push-deferred-post-mvp`.
7. Activity tracking — **built/shipped (PR #43).** `/impact` "Your Impact" surface: per-user calls + scripts counts and a reverse-chron call history over `call_events` (bill title + rep name from stored FKs; rows link to `/bills/[id]`) + empty state; personal-only. Dead Callenge nav tab → Your Impact. **Followed-bills section/Follow button deferred** (no `followed_bills` writer exists yet); scripts is **count-only** for v1.

**Design:** `/bills/[id]` is **floor + ceiling complete**; the landing is **floor + ceiling complete** (#51); and **every other non-landing route is now floor-cohered onto the system** (#52 — primitives, serif titles, wordmark, neutral pills, type-scale; V4 card on `/bills` + `/dashboard`). All recorded in `docs/DESIGN_DECISIONS.md`. **Next on design:** a `creative-director` **ceiling** pass on the now-cohered app (feed card + dashboard especially) — the floor is done, the ceiling is owed. The a11y contrast deferrals on the V4 card (`ink-50` title, `#FAF8F5` border) remain v2-deferred per `PRODUCT.md`. The **wordmark is wired** app-wide; logo/favicon/full brand identity work is otherwise separate.

## Next action (single)
The app UI cohesion pass shipped (#52) — every non-landing route is now floor-cohered onto the system. **Next: TBD — Colby's call.** Candidates from current docs:
- **`creative-director` ceiling on the cohered app** (`docs/DESIGN_DECISIONS.md → post-critique` deferral): the floor is done; the feed card + dashboard especially are owed a ceiling pass. The `/critique` (29/40) surfaced this as the clean next design step.
- **Steady-state `ai_summary` + `ai_headline` pipeline** (`docs/deferred.md#steady-state-summarize-cron`, `#feed-card-v4-build`): the one-off backfills shipped (PRs #45/#47 — 480/482 filled) and the feed now reads `ai_headline`, but auto-generating summary + headline for new/changed bills as the sync ingests them was never built — bills synced after the backfill land summary/headline-null (degrade state). Coupled to re-arming the (currently unscheduled) bill-sync cron.
- **V4 coverage + feed-card ceiling** (`docs/deferred.md#feed-card-v4-build`): the filled-headline + interest-pill paths are mockup/live-data verified but not auto-tested (the committed e2e covers the degrade state only); the feed-card **ceiling** pass is still owed.

**Sequenced out — not near-term (by decision, 2026-06-05):**
- **Feature 6 — Web push:** **DEFERRED to post-MVP — no longer a V1/MVP item or a "next action" candidate.** Schema exists (`push_subscriptions`, `notifications_sent`) but nothing's built; coupled to re-arming the sync cron. `docs/deferred.md#web-push-deferred-post-mvp`.
- **`email-verification-deferred` — FINAL V1 LAUNCH GATE:** action it **last**, only after all other V1 work is complete (flip Confirm-email ON — ~zero work — or a custom Resend flow); NOT a near-term/next-up item until then. `noindex-pre-launch` removal is the same launch-eve gate.

## Open decisions / debt (see docs/deferred.md)
- **`feature-3-issue-tags-coverage-gap` — RESOLVED (CRS re-anchor, PR #41).** Was 377/482 untagged; now 0.2% (481/482 tagged, 0 on old ids) via migration 007 + the G4 backfill (2026-06-02). Remaining open: **`taxonomy-crs-reassess`** — revisit category granularity/labels at **500 users or 2026-08-15**.
- **Summary accuracy + steady-state pipeline** — see the flag; broad backfill **shipped (PR #45 — 480/482)**; the steady-state summarize cron stays deferred (`steady-state-summarize-cron`) and an accuracy spot-check on truncated bills is owed.
- `email-verification-deferred` — **FINAL V1 LAUNCH GATE** (re-tagged 2026-06-05): sequence it **last**, only after all other V1 work is complete; not a near-term item until then. Flip Confirm-email ON (~zero work) or a custom Resend token flow.
- **`ai-disclaimer-decoded-hero` — RESOLVED (PR #34):** disclaimer shipped in the Decoded card.
- **Design / brand:** bill-detail + landing ceilings **done**; the whole app is now **floor-cohered** (#52) and the V4 card is on feed + dashboard. Remaining: a `creative-director` **ceiling** on the cohered app; `type-scale-extension` (20/30px + non-uppercase-12px caption gaps — tokens-only, resolve as surfaces are touched); `landing-features-grid-emoji`. Wordmark is wired; logo/favicon/full brand identity otherwise still open.
- **`consolidation-followup-offscope-slate-and-semantic-colors` — RESOLVED (PR #52).** The whole-app cohesion pass cleared the remaining `slate-*` (`ImpactMetrics`/`RepCard`), mapped every off-palette red/green banner onto the `Alert` primitive (oxblood/moss), and deleted the partisan `partyColor()`. Repo-wide audit: 0 `slate-*` / `red-*`/`green-*` / raw `text-xs` / `bg-white` in app+component `.tsx`. Only sibling debt left: `lib/utils.ts` `urgencyLabel().color` still returns unused off-palette tints (the cards never consume it).
- `landing-copy-out-of-scope-features` — **SCOPE.** Landing still advertises state/local reps + the "Callenge" gamification (out of MVP); reword + drop the nav item before public/donor launch.
- `onboarding-skip-not-gated` — product decision on nudging incomplete-profile users.
- `untyped-browser-supabase-client` — `lib/supabase/client.ts` returns `null as any`; client queries unchecked at compile time (v1.1).
- **`call-events-cascade-durability`** (NEW this session — DEBT, latent) — `call_events` is `ON DELETE CASCADE` on both `bill_id`/`representative_id` (the shared `bills`/`representatives` caches), so any future bill/rep prune would cascade-delete users' call history. Audited: **no deleter exists today** (ingestion is upsert-only). Becomes BLOCK the moment a prune is added; fix is its own migration (loosen the two FKs, or snapshot title/name onto `call_events`).
- **`e2e-tests-seed-live-prod`** (NEW this session — Launch gate) — no separate test DB: the committed e2e suite seeds the **live prod** project and relies on `afterAll` cleanup, so a crash mid-run can leave namespaced throwaway rows in prod (briefly in the live feed). Trigger: stand up a separate test project before public launch.
